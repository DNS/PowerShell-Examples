Get-Command | Select-String 'disk|volume|partition|psdrive'

Get-Disk
Get-Partition
Get-Volume
Get-PSDrive

-----------------------------
# FORMAT DRIVE

# Get available drive
Get-Volume

# Quick format
Format-Volume -DriveLetter X -FileSystem NTFS -Compress
Format-Volume -DriveLetter X -FileSystem exFAT

# Full wipe and then format
Format-Volume -DriveLetter X -FileSystem NTFS -Compress -Full

--------------------------------
# DRIVE LETTER

# Map a network share to drive Z:
New-PSDrive -Name Z -PSProvider FileSystem -Root \\Server\Share -Persist
